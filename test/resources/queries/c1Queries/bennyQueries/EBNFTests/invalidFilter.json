{
	"title": "should reject query because WHERE has an invalid filter",
	"input":  {
		"WHERE": {
			"COLUMNS": {
				"courses_avg": 91
			}
		},
		"OPTIONS": {
			"COLUMNS": ["courses_dept", "courses_id", "courses_avg"],
			"ORDER": "courses_avg"
		}
	},
	"with": "InsightError",
	"errorExpected": true
}
